[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npx vite preview --port $PORT --host"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Disable auto install - we do it in buildCommand
[phases.install]
cmds = []

